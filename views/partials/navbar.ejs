<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/">
        <% if (lang === 'ru') { %>
            <h3>Главная</h3>
        <% } else { %>
            <h3>Home</h3>
        <% } %>
    </a>
    <button class="navbar-toggler collapsed ml-auto" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav ml-auto">
            <% if (isAuthenticated) { %>
                <li class="nav-item">
                    <a class="navbar-brand" href="/profile">
                    <% if (lang === 'ru') { %>
                        <h3>Профиль</h3>
                    <% } else { %>
                        <h3>Profile</h3>
                    <% } %>
                    </a>
                </li>
            <% } %>
            <% if (!isAuthenticated) { %>
            <li class="nav-item">
                <a class="navbar-brand" href="/users/login">
                <% if (lang === 'ru') { %>
                    <h3>Войти</h3>
                <% } else { %>
                    <h3>Login</h3>
                <% } %>
                </a>
            </li>

            <li class="nav-item">
                <a class="navbar-brand" href="/users/register">
                <% if (lang === 'ru') { %>
                    <h3>Регистрация</h3>
                <% } else { %>
                    <h3>Register</h3>
                <% } %>
                </a>
            </li>
            <% } %>
            <% if (isAuthenticated) { %>
            <li class="nav-item">
                <a class="navbar-brand" href="/users/logout">
                    <% if (lang === 'ru') { %>
                        <h3>Выйти</h3>
                    <% } else { %>
                        <h3>Logout</h3>
                    <% } %>
                </a>
            </li>
            <% } %>
            <li class="nav-item dropdown mt-1">
                <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <% if (lang === 'ru') { %>
                                <strong><div id="lang">RU</div></strong>
                            <% } else { %>
                                <strong><div id="lang">EN</div></strong>
                            <% } %>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <form action="/lang/en" method="get">
                        <button class="dropdown-item" type="submit"><div id="en">EN</div></button>
                    </form>
                    <form action="/lang/ru" method="get">
                        <button class="dropdown-item" type="submit"><div id="ru">RU</div></button>
                    </form>
                </div>
            </li>

        </ul>
    </div>
</nav>
